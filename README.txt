GISCup14_C
==========

Code is in C.  Compile code by running make in the src directory.  Run code by typing Simplify < PointToRemove > < LineInputFilePath > < PointInputFilePath > < OutputFilePath > on the command line
